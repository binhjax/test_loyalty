package types

import (
  // "fmt"
  // "strings"
  // "time"
  // "encoding/json"
)

type TransactionResult struct {
  Nonce uint64 `json:"username"`
  TransactionHash string `json:"TransactionHash"`
}
